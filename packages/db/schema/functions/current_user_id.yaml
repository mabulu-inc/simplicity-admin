name: current_user_id
language: sql
returns: uuid
replace: true
body: |
  SELECT current_setting('app.user_id')::uuid;
grants:
  - to: anon
    privileges: [EXECUTE]
  - to: app_viewer
    privileges: [EXECUTE]
  - to: app_editor
    privileges: [EXECUTE]
  - to: app_admin
    privileges: [EXECUTE]
