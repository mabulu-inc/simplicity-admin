# T-070: Server-side RBAC enforcement on mutations

- **Status**: DONE
- **Milestone**: M5 — Security Hardening
- **PRD Reference**: —
- **Depends**: T-068, T-088
- **Completed**: 2026-03-16 17:48
- **Commit**: (pending)
- **Produces**:
  - Updated `packages/ui/src/routes/(app)/[table]/[id]/+page.server.ts` — RBAC checks in `update`, `delete`, `transition` actions
  - Updated `packages/ui/src/routes/(app)/[table]/new/+page.server.ts` — RBAC check in `default` action
  - New `packages/ui/src/lib/server/rbac-guards.ts` — reusable RBAC guard functions
- **Tests**: `packages/ui/tests/security/rbac-mutations.test.ts`
  - POST to update action without permission returns 403
  - POST to delete action without permission returns 403
  - POST to create action without permission returns 403
  - POST to update action with read-only column is rejected
  - Unauthenticated POST returns 401
- **AC**: All mutation endpoints enforce RBAC server-side, not just via UI

## Completion Notes

Added server-side RBAC enforcement on all mutation form actions per B-SEC-002:
- Created `rbac-guards.ts` with `requireAuth` (401), `requireInsert`/`requireUpdate`/`requireDelete` (403), and `getWritableColumnNames` guards
- `update` action: requires auth, checks canUpdate, filters columns to writable set (excluding read-only)
- `delete` action: requires auth, checks canDelete via RBAC
- `transition` action: requires auth (was using optional chaining before)
- `create` (new) action: requires auth, checks canInsert, filters columns to writable set
- 12 tests covering all guard functions and edge cases

## Hints

- Read docs/specs/security.md (B-SEC-002) for exact interfaces and behavior specs