# T-080: GraphQL depth limiting

- **Status**: DONE
- **Completed**: 2026-03-16
- **Completion Notes**: Added GraphQL depth limiting via a Graphile Config plugin with parseAndValidate middleware. Default max depth is 10, configurable via `api.maxQueryDepth`. Added `createDepthLimitRule` validation rule and `makeDepthLimitPlugin` factory. Tests cover depth within/exceeding limits, fragments, and inline fragments.
- **Milestone**: M5 — Security Hardening
- **PRD Reference**: —
- **Depends**: T-068, T-088, T-089
- **Produces**:
  - Updated `packages/api/src/graphql/preset.ts` — depth limiting plugin
  - Updated `packages/core/src/config/types.ts` — `api.maxQueryDepth` option
- **Tests**: `packages/api/tests/depth-limit.test.ts`
  - Query within depth limit succeeds
  - Query exceeding depth limit is rejected with error
  - Default depth limit is 10
- **AC**: Deeply nested GraphQL queries are rejected


## Hints

- Read docs/specs/security.md (B-SEC-012) for exact interfaces and behavior specs