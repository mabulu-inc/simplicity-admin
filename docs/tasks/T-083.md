# T-083: Replace z.any() with typed schemas in core config

- **Status**: DONE
- **Completed**: 2026-03-16T20:12:00Z
- **Commit**: 30cb36381fda7ec36c794192f29a6abdf9ac087b
- **Cost**: $0.63
- **Completion Notes**: Replaced all 5 z.any() usages with typed Zod schemas: authStrategyImplSchema for strategy, tableHooksSchema for hooks, tableActionSchema for actions, providerOverridesSchema for providers, pluginSchema for plugins. Added 15 new test cases validating rejection of invalid shapes.
- **Milestone**: M6 — Code Quality & Hardening
- **PRD Reference**: §2, §5
- **Depends**: T-003, T-088, T-089
- **Produces**:
  - Updated `packages/core/src/config/schema.ts` — `strategy`, `hooks`, `actions`, and `plugins` fields use specific Zod schemas instead of `z.any()`
- **Tests**: `packages/core/tests/config.test.ts` — add cases that reject invalid hook/action/plugin shapes; existing config tests still pass
- **AC**: No `z.any()` remains in config schema; invalid configs are rejected at parse time


## Hints

- Read docs/specs/core.md for exact interfaces and behavior specs