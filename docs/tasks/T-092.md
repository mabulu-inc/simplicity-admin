# T-092: README and GitHub Pages documentation site

- **Status**: DONE
- **Milestone**: M6 — Code Quality & Hardening
- **Depends**: none
- **PRD Reference**: §7
- **Complexity**: heavy
- **Completed**: 2026-03-17
- **Commit**: af7d19f709281cfed14a50ad42658f7c7b034dea
- **Cost**: $1.59
- **Completion Notes**: Created README.md, Astro+Starlight docs-site with 19 content pages (Getting Started, Core Concepts, Packages, Guides, Reference), deploy-docs.yml GitHub Pages workflow, and comprehensive test suite (31 tests).

## Description

The repo is now public under BSL 1.1 (per PRD §7). The LICENSE file already exists. Create the README and documentation site.

Use the Ralph docs site (`../ralph/docs-site/`) as the reference implementation — same stack (Astro + Starlight), same deployment pattern (GitHub Pages via `deploy-docs.yml` workflow), standalone `docs-site/` directory with its own `pnpm-lock.yaml`.

**Note:** Tests already exist at `packages/core/tests/docs-and-license.test.ts` from a prior attempt. The LICENSE tests will pass. Focus on making the README and docs-site tests pass.

### README.md

Pragmatic, developer-focused. Should include:
- One-liner from PRD §1 vision
- Quick start (init, configure, run)
- Feature highlights (zero-config, schema-as-truth, RBAC, multi-tenancy, state machines, notifications, dashboards, provider pattern)
- Package table (core, db, auth, api, ui, cli)
- Link to docs site
- Development instructions (`pnpm install && pnpm check`)
- License summary with link to LICENSE file

### GitHub Pages docs site

Standalone Astro + Starlight site in `docs-site/` (not part of the pnpm workspace). Deploy via `.github/workflows/deploy-docs.yml` on pushes to `main` that touch `docs-site/**`.

Site structure — pragmatic and comprehensive, aimed at developers:

- **Getting Started**: Introduction, Quick Start, Configuration
- **Core Concepts**: Architecture (provider pattern, package structure), Schema-as-Truth, RBAC (code ceiling, functional roles, column-level grants)
- **Packages**: One page per package (core, db, auth, api, ui, cli) with purpose, exports, and usage
- **Guides**: Adding a Table, Customizing Auth, Writing Plugins, Embedding in Express/Fastify
- **Reference**: Config options, CLI commands, Environment variables

Content should be derived from `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/specs/*.md`, and the actual source code. Do not invent features that don't exist — document what's built.

## Produces

- `README.md`
- `docs-site/` — Astro + Starlight documentation site (package.json, astro.config.mjs, tsconfig.json, pnpm-lock.yaml, src/content/docs/**)
- `.github/workflows/deploy-docs.yml`

## AC

- `README.md` renders correctly on GitHub with no broken links
- `docs-site/` builds successfully (`cd docs-site && pnpm install && pnpm build`)
- Deploy workflow targets `docs-site/**` path and uses GitHub Pages deployment (actions/upload-pages-artifact + actions/deploy-pages)
- Site base path is `/simplicity-admin`
- Sidebar navigation covers all sections listed above
- No invented features — all content reflects actual implemented functionality
- All tests in `packages/core/tests/docs-and-license.test.ts` pass
